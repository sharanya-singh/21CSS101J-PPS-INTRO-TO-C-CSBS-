# include <stdio.h>
void printString(char arr[]) ;
int main(){
 char Name[50];
 scanf("%s",Name);
 printf("your Name is %s", Name);
return 0;
}
void printString(char arr[]) {
for(int i=0; arr[i] != '\0'; i++) {
printf("%c", arr[i]);
}
printf("\n");
}
