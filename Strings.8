#include <stdio.h>
#include <string.h>
void printString(char arr[]);
int count Length (char arr[]);
int main() {
printf("RA2211042010010\n");
char oldStr[] = "oldStr";
char newStr[] = "newStr";
strcpy(newStr, oldStr);
puts (newStr);
}
